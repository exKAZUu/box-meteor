name: meteor
version: 0.0.2
inherits: exkazuu/meteor@0.0.1
type: main
platform: ubuntu@14.04
keywords:
    - nodejs
    - meteor
    - phantomjs
script: |
    sudo apt-get -y install build-essential
    nvm install stable
    nvm use stable
    nvm alias default stable
    git clone git://github.com/ariya/phantomjs.git
    cd phantomjs
    git checkout 2.0
    yes | ./build.sh
    curl https://install.meteor.com/ | sh
    nvm version
    node -v
    phantomjs -v
    meteor --version